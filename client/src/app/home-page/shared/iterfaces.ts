export interface erpMenu  {
    name: string,
    activeClass: boolean,
    img: string,
    prompt: string
}

export interface tab {
    name: string,
    active: boolean,
    code: string
}

export interface slide {
    name: string,
    activeClass: boolean
}
